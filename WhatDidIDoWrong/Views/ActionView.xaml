<UserControl x:Class="WhatDidIDoWrong.Views.ActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WhatDidIDoWrong.Views"
             mc:Ignorable="d" 
             DataContext="{Binding Main, Source={StaticResource Locator}}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="6,0,6,0" Height="Auto">
        <TextBox Name="ActionName"
                 Height="Auto" VerticalAlignment="Top" 
                 Background="{Binding HeaderColor}" Foreground="White" FontSize="18"
                 Text="{Binding ActionName}"></TextBox>
    </Grid>
</UserControl>
